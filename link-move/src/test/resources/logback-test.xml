<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{20} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.nhl.link.move.log" level="ALL"/>

    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>

